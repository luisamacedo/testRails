<%= form_with(model: lawyer, local: true) do |form| %>
  <% if lawyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lawyer.errors.count, "error") %> prohibited this lawyer from being saved:</h2>

      <ul>
      <% lawyer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :state_id %>
    <%= collection_select(:lawyer, :state_id, @state_options_for_select, :id, :name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
